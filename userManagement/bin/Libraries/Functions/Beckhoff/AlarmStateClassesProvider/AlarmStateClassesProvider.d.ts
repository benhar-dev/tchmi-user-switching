declare module TcHmi {
    module Functions {
        module Beckhoff {
            function AlarmStateClassesProvider(rowValue: TcHmi.Server.Events.Event, dataIndex: number, rowNumber: number): string[];
        }
    }
}
